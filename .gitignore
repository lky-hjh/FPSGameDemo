# Visual Studio
*.VC.db
*.VC.VC.opendb
*.suo
*.user
*.userosscache
*.sln.docstates
*.vcxproj.filters

# ---------------------------
# .gitignore for FPSGameDemo (Unreal Engine)
# Tailored for Windows + Visual Studio + common editors
# Keep project/source files (.uproject, Source/, Content/, Config/) tracked.
# Ignore build artifacts, user-specific files, IDE caches and OS temp files.
# ---------------------------

# ---------------------------
# IDE / Editor
# ---------------------------
.vs/                    # Visual Studio workspace cache
*.suo
*.user
*.userosscache
*.VC.db
*.VC.VC.opendb
*.sdf
*.opensdf
.vscode/                # VS Code workspace settings (usually user-specific)
.idea/                  # Rider / IntelliJ

# Keep solution and project files in repo (do NOT ignore .sln/.vcxproj)
# If you intentionally want to ignore certain generated project files, add them explicitly.

# ---------------------------
# Unreal Engine-specific
# ---------------------------
# Engine / build artifacts produced by Unreal
Binaries/
Build/
Intermediate/
DerivedDataCache/
Saved/

# Packaging / staging
*.pak
PackagedBuilds/
Stage/

# Shader cache and generated global shader binaries
GlobalShaderCache-*.bin

# Asset registry and other generated asset files
AssetRegistry.bin

# Runtime-generated content (profiles, benchmarks, logs)
Saved/Logs/
Saved/Profiling/
Saved/Benchmarking/

# Crash reports
Crashes/

# ---------------------------
# Build / Compiler artifacts
# ---------------------------
*.obj
*.o
*.a
*.lib
*.dll
*.exe
*.so
*.dylib
*.pdb
*.idb
*.ipdb
*.ilk
*.exp
*.iobj
*.tlog
*.lastbuildstate

# Temporary/backup files
*.tmp
*.temp
*.swp
*~

# ---------------------------
# Logs, caches, and OS files
# ---------------------------
*.log
.DS_Store
Thumbs.db

# ---------------------------
# Project & user config (local settings)
# ---------------------------
# Per-user Unreal project settings (do not commit)
*.uproject.user

# Local build/config overrides (if you have local-only config files, add here)
Config/LocalBuilds/
Config/Windows/
Config/WindowsEditor/

# Editor generated (do not commit)
*.generated.*

# ---------------------------
# Optional: Visual Studio cache files
# ---------------------------
*.sln.docstates
*.sln.DotSettings
*.DotSettings.user

# ---------------------------
# Keep these files tracked (explicit negations if needed)
# E.g., if you want to keep some files under Config or .vscode, add rules like:
# !Config/DefaultEngine.ini
# !.vscode/launch.json
# ---------------------------

# End of file
Content/TFPMovement/
